<Location /server-status>
    SetHandler server-status
    Require local
</Location>


#What this file is for is to turn on mod_status which is always turned off by default but intentionally turned on because we are using apache-exporter
#to get live metrics from apache about the behaviour of our website in prometheus format