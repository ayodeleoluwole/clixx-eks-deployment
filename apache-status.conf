<Location /server-status>
    SetHandler server-status
    Require local
</Location>


#when a request comes in for this specific path, use the status module to handle it. It restricts access to localhost only, since apache-exporter
# runs as a sidecar in the same pod and reaches this over localhost